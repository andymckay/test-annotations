name: ⚠

on: [push, repository_dispatch, workflow_dispatch]

jobs:
    build:
      runs-on: ubuntu-latest
      steps:
      - name: Annotate ✒
        run: |
          echo "::group::My title"
          echo "Inside group"
          echo "::endgroup::"
          echo ::error::User issued error!
          echo ::warning line=1,col=4,file=insecure.sh::🚨There are security 🔒 issues with this repository. 🚨
